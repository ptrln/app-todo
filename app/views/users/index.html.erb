<%= breadcrumb_helper([["Users", nil]]) %>
<legend>
  <h1>
    Users
    <small>
      <%= link_to("Create new user", new_user_path) %>
    </small>
  </h1>
</legend>
<% @users.each do |user| %>
<p>
<%= link_to(user.name, user_path(user)) %>
</p>
<% end %>

